const endpoints = {
  login: '/users/login',
  currentUser: '/users/me',
  createProject: '/project',
  projects: '/projects',
  projectCount: '/project/count',
  setup: '/setup',
};

export default endpoints;
