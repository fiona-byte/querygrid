const endpoints = {
  login: '/users/login',
  currentUser: '/users/me',
  createProject: '/project',
  projects: '/projects',
  projectCount: '/project/count',
  setup: '/setup',
  install: '/install',
};

export default endpoints;
